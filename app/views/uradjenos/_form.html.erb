<%= form_with(model: uradjeno, local: true) do |form| %>
  <% if uradjeno.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(uradjeno.errors.count, "error") %> prohibited this uradjeno from being saved:</h2>

      <ul>
        <% uradjeno.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
